#ifndef MATH_UTILS_H
#define MATH_UTILS_H

#define align_to(size, align) (((size) * (align) + (align) - 1) / align)
#define min(a, b) ((a) < (b) ? (a) : (b))
#define max(a, b) ((a) < (b) ? (b) : (a))


#endif
